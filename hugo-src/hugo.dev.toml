# Surcharge DEV (hugo server) — chargé après hugo.toml :
#   hugo server --config hugo.toml,hugo.dev.toml --port 1313
[params]
  apiBase = "auto"   # = http://<hôte de la page>:8001/api — marche en local, sur le LAN et via WireGuard
